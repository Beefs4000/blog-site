{{> nav}}

<div class="text-center">
    <h5 class="title is-4">View your old posts</h5>
    {{#each userPosts as |post|}}
    {{> dashboard-post}}
    {{/each}}
    <a href="/newpost"><button class="button is-black">Make a post!</button></a>

</div>

{{#if session.loggedIn}}

<script src="./js/log-out.js"></script>


{{/if}}